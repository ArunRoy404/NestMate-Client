import ThemeToggle from "./ThemeToggle/ThemeToggle";

const Navbar = () => {
    return (
        <nav>
            <h1>navbar</h1>
            <ThemeToggle />
        </nav>
    );
};

export default Navbar;